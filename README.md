# Gabriel Granato Academic Website

This repository contains Gabriel Granato's academic website, built from the Academic Pages GitHub Pages template.

Local preview:

```bash
bundle install
bundle exec jekyll serve --drafts -H localhost --config _config.yml,_config_docker.yml
```
